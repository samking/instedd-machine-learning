<p>
  <b>Table UUID:</b>
  <%=h @dataset.table_uuid %>
</p>

<h2>Is the service still learning?</h2>
<p><%= @dataset.is_learning? %></p>

<h2>Attributes</h2>
<% @dataset.database_table.as_map.each do |key, val| %>
  <p><%= [key, val].inspect %></p>
<% end %>

<%= link_to 'Destroy', @dataset, :confirm => 'Are you sure?', :method => :delete %></td>
<%= link_to 'Back', datasets_path %>
<p><%= form_tag(dataset_path, :method => "put") %>
  UUID of row to delete<%= text_field_tag 'remove_rows' %><br />
  Column to remove from row (blank deletes entire row)<%= text_field_tag 'remove_cols' %><br />
  URL of Data to Add<%= text_field_tag 'data_url' %><br />
  Service to Use (supported: calais)<%= text_field_tag 'service' %><br />
  <%= submit_tag 'Modify Data and/or run Machine Learning' %>
</p>



