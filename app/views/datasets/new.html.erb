<h1>New dataset</h1>

<% form_for(@dataset) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :table_name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', datasets_path %>
